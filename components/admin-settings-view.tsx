'use client';

import { FormEvent, useEffect, useState } from 'react';
import Link from 'next/link';
import { Building2, Check, FileCog, Globe2, Save, ShieldCheck, Users } from 'lucide-react';
import { readLocal, writeLocal } from '@/lib/farm-storage';

export function AdminSettingsView() {
  const [saved, setSaved] = useState(false);
  const [values, setValues] = useState<Record<string, string>>({});
  useEffect(() => setValues(Object.fromEntries(Object.entries(readLocal<Record<string, unknown>>('agroflux-settings-global', {})).map(([key, value]) => [key, String(value)]))), []);
  function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const next = Object.fromEntries(Array.from(form.entries()).map(([key, value]) => [key, String(value)])); writeLocal('agroflux-settings-global', { ...next, savedAt: new Date().toISOString() }); setValues(next); setSaved(true); window.setTimeout(() => setSaved(false), 5000); }
  const field = (name: string, label: string, value: string, type = 'text') => <label className="block"><span className="field-label">{label}</span><input name={name} type={type} className="input-base" defaultValue={values[name] ?? value} /></label>;
  return <div className="fade-in space-y-7"><div><p className="eyebrow mb-2">Administration · Configuration globale</p><h1 className="page-title">Réglages globaux du SaaS</h1><p className="muted mt-2 max-w-2xl text-[13px]">Ces informations concernent SCOOPS LE REVEIL dans son ensemble. Les paramètres propres à chaque unité restent dans leur espace respectif.</p></div><div className="grid gap-3 sm:grid-cols-4"><AdminSettingLink href="/dashboard-admin/utilisateurs" icon={Users} label="Utilisateurs globaux" /><AdminSettingLink href="/dashboard-admin/unites" icon={Building2} label="Gestion des unités" /><AdminSettingLink href="/dashboard-admin/communication" icon={Globe2} label="Communication publique" /><AdminSettingLink href="/dashboard-admin/parametres" icon={FileCog} label="Réglages généraux" active /></div><form key={`settings-${String(values.savedAt ?? 'default')}`} onSubmit={save} className="surface p-5 sm:p-7"><div className="flex items-start gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#edf8e9] text-[#5b9d5b]"><Building2 size={18} /></span><div><h2 className="text-[17px] font-bold text-ink">Identité de la société</h2><p className="mt-1 text-[11px] text-[#87958d]">Ces informations sont utilisables par les pages et documents publics.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{field('companyName', 'Nom légal', 'SOCIETE COOPERATIVE SIMPLIFIEE')}{field('companyBrand', 'Nom commercial', 'SCOOPS LE REVEIL')}{field('companyCity', 'Ville', 'Obala')}{field('companyPhone', 'Téléphone', '')}{field('companyEmail', 'Email', '', 'email')}<label className="block sm:col-span-2"><span className="field-label">Adresse</span><textarea name="companyAddress" className="input-base min-h-[80px] resize-none" defaultValue={values.companyAddress ?? 'Obala, Centre, Cameroun'} /></label></div><div className="mt-6 flex justify-end"><button type="submit" className="btn-primary"><Save size={15} /> Enregistrer les modifications</button></div>{saved && <div role="status" className="mt-4 flex items-center justify-end gap-2 rounded-xl border border-[#cde8c7] bg-[#effaeb] px-3 py-2.5 text-[11px] font-bold text-[#4d8f51]"><Check size={14} /> Modifications enregistrées et conservées.</div>}</form><form key={`settings-${String(values.savedAt ?? 'default')}`} onSubmit={save} className="surface p-5 sm:p-7"><div className="flex items-start gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#edf3ff] text-[#6385bd]"><ShieldCheck size={18} /></span><div><h2 className="text-[17px] font-bold text-ink">Politique de session</h2><p className="mt-1 text-[11px] text-[#87958d]">Le compte MOVIC reste protégé côté serveur par un jeton administrateur signé.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="block"><span className="field-label">Durée de session affichée</span><select name="sessionDuration" className="input-base" defaultValue={values.sessionDuration ?? '7'}><option value="1">1 jour</option><option value="7">7 jours</option><option value="30">30 jours</option></select></label><label className="block"><span className="field-label">Double validation</span><select name="twoFactor" className="input-base" defaultValue={values.twoFactor ?? 'off'}><option value="off">Désactivée</option><option value="on">Activée</option></select></label></div><div className="mt-6 flex justify-end"><button type="submit" className="btn-primary"><Save size={15} /> Enregistrer la sécurité</button></div></form></div>;
}
function AdminSettingLink({ href, icon: Icon, label, active }: { href: string; icon: typeof Users; label: string; active?: boolean }) { return <Link href={href} className={`flex items-center gap-2 rounded-xl border px-3 py-3 text-[11px] font-bold transition ${active ? 'border-[#bdd9bc] bg-[#edf8e9] text-[#4d8f52]' : 'border-[#edf0eb] bg-white text-[#718078] hover:border-[#cde3cc] hover:text-ink'}`}><Icon size={15} />{label}</Link>; }
